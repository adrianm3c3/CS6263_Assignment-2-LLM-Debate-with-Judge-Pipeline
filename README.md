# LLM Debate + Judge Pipeline

## Overview
This project implements a multi-agent LLM debate system consisting of:
- Debater A
- Debater B
- Judge

The goal is to evaluate whether structured debate improves reasoning accuracy compared to baseline methods.

---

## Setup

Install dependencies:

```bash
pip install -r requirements.txt
